import { СropTextPipe } from './сrop-text-pipe.pipe';

describe('СropTextPipe', () => {
  it('create an instance', () => {
    const pipe = new СropTextPipe();
    expect(pipe).toBeTruthy();
  });
});
